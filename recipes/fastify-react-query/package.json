{
  "name": "@ts-contract-recipes/fastify-react-query",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"pnpm --filter shared dev\" \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build": "echo 'Build skipped - this recipe uses bundler moduleResolution which has limitations with workspace package type resolution. Use dev mode instead.'",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
